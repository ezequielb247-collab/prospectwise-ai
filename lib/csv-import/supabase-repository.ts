import "server-only";
import type {SupabaseClient} from "@supabase/supabase-js";
import {duplicateKeys} from "./parser";
import type {CsvLeadRepository} from "./service";
import type {NormalizedCsvLead} from "./types";
export class SupabaseCsvLeadRepository implements CsvLeadRepository{constructor(private readonly client:SupabaseClient){}async campaignBelongsToUser(userId:string,campaignId:string){const {data,error}=await this.client.from("campaigns").select("id").eq("user_id",userId).eq("id",campaignId).maybeSingle();if(error)throw error;return Boolean(data)}async existingDuplicateKeys(userId:string){const {data,error}=await this.client.from("leads").select("name,phone,website,address,city,state,category,rating,reviews,maps_url,instagram,facebook").eq("user_id",userId);if(error)throw error;const keys=new Set<string>();for(const row of data??[]){const lead:NormalizedCsvLead={name:row.name,phone:row.phone,website:row.website,address:row.address,city:row.city,state:row.state,category:row.category,rating:row.rating,reviews:row.reviews,mapsUrl:row.maps_url,instagram:row.instagram,facebook:row.facebook,provider:"csv_import"};duplicateKeys(lead).forEach(key=>keys.add(key))}return keys}async importLeads(_:string,campaignId:string,leads:NormalizedCsvLead[]){if(!leads.length)return [];const {data,error}=await this.client.rpc("import_csv_leads",{p_campaign_id:campaignId,p_leads:leads});if(error)throw error;return (data??[]).map((row:{lead_id:string})=>row.lead_id)}}
