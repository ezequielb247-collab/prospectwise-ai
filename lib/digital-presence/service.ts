import type{DigitalPresence}from"./types";export interface DigitalPresenceRepository{get(userId:string,leadId:string):Promise<DigitalPresence|undefined>;save(userId:string,value:DigitalPresence):Promise<DigitalPresence>;ownsLead(userId:string,leadId:string):Promise<boolean>};export class DigitalPresenceService{constructor(private repo:DigitalPresenceRepository){}get(userId:string,leadId:string){return this.repo.get(userId,leadId)}async save(userId:string,value:DigitalPresence){if(!await this.repo.ownsLead(userId,value.leadId))throw new Error("Lead não encontrado.");if(value.websiteStatus==="present"&&!value.websiteUrl)throw new Error("Informe a URL quando o site estiver presente.");return this.repo.save(userId,{...value,websiteUrl:value.websiteUrl?.trim()||null,domain:value.domain?.trim()||null,confidence:Math.max(0,Math.min(100,value.confidence)),source:"manual",lastCheckedAt:new Date().toISOString()})}}
