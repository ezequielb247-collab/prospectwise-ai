export interface PersistentRepository{createCampaign(input:{name:string;city:string;state:string;segment:string;companyLimit:number;dailyLimit:number;services:string[]}):Promise<string>;moveLead(input:{leadId:string;campaignId:string;stage:string}):Promise<void>;prepareMessage(input:{leadId:string;campaignId:string;body:string}):Promise<string>}
export class PersistenceService{constructor(private readonly repository:PersistentRepository){}createCampaign(input:Parameters<PersistentRepository["createCampaign"]>[0]){return this.repository.createCampaign(input)}moveLead(input:Parameters<PersistentRepository["moveLead"]>[0]){return this.repository.moveLead(input)}prepareMessage(input:Parameters<PersistentRepository["prepareMessage"]>[0]){return this.repository.prepareMessage(input)}}
