import type{MessageContext,ValidationIssue}from"./types";export const OUTREACH_STYLES=["curta","direta","consultiva","site","whatsapp","automacao","follow_up_1","follow_up_2","retorno_interesse"]as const;export type OutreachStyle=typeof OUTREACH_STYLES[number];export const CTA_OPTIONS=["Posso te mostrar uma ideia?","Posso enviar um exemplo?","Faz sentido conversarmos por alguns minutos?","Posso preparar uma demonstração sem compromisso?"]as const;export type OutreachVariant="A"|"B";export function generateDeterministicOutreach(context:MessageContext,options:{style:OutreachStyle;cta:string;variant:OutreachVariant}){const{lead,analysis}=context,issues:ValidationIssue[]=[];if(["Opt-out","Cliente","opt_out","client"].includes(lead.crmStage))issues.push({type:"blocking_error",message:"Lead bloqueado para prospecção."});if(!lead.name.trim())issues.push({type:"blocking_error",message:"Empresa sem nome."});const city=lead.city?` em ${lead.city}`:"";if(!lead.city)issues.push({type:"warning",message:"Cidade não informada."});const recommendation=analysis?.recommendedServices[0];const reason=analysis?.mainReason;const verifiedNoSite=lead.website===null;const opening=options.variant==="A"?`Olá! Encontrei a ${lead.name}${city}.`:`Olá, tudo bem? Estou entrando em contato com a ${lead.name}.`;let insight="";if(options.style==="site"&&verifiedNoSite)insight=" Não encontrei um site próprio nos dados verificados.";else if(options.style==="whatsapp"&&lead.phone)insight=" Vi uma oportunidade de organizar melhor o atendimento digital.";else if(recommendation)insight=` Identifiquei uma oportunidade relacionada a ${recommendation.toLowerCase()}.`;else if(reason)insight=` Os dados indicam uma oportunidade digital: ${reason}.`;else{insight=" Gostaria de entender se existe alguma prioridade digital no momento.";issues.push({type:"warning",message:"Abordagem sem análise comercial disponível."})}const bridge=options.style==="direta"?" Trabalho com soluções digitais para empresas locais.":options.style.startsWith("follow_up")?" Retomando nosso contato anterior.":" Trabalho com melhorias digitais de forma consultiva.";const body=`${opening}${bridge}${insight} ${options.cta}`.replace(/\s+/g," ").trim();return{body,variant:options.variant,style:options.style,warnings:issues}}
