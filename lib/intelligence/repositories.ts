import type {LeadAnalysis,LeadIntelligenceInput} from "./types";
export interface LeadAnalysisRepository{findByLeadId(leadId:string):Promise<LeadAnalysis|undefined>;findByCampaignId(campaignId:string):Promise<LeadAnalysis[]>;list():Promise<LeadAnalysis[]>;upsert(analysis:LeadAnalysis):Promise<LeadAnalysis>}
export interface LeadIntelligenceDataSource{findLeadById(leadId:string):Promise<LeadIntelligenceInput|undefined>;findLeadsByCampaignId(campaignId:string):Promise<LeadIntelligenceInput[]>;listLeads():Promise<LeadIntelligenceInput[]>}
export interface IntelligenceActivityRepository{record(activity:{type:"lead_score_calculated"|"lead_analysis_recalculated";leadId:string;campaignId:string;createdAt:string}):Promise<void>}
export class InMemoryLeadAnalysisRepository implements LeadAnalysisRepository{private records=new Map<string,LeadAnalysis>();async findByLeadId(id:string){return this.records.get(id)}async findByCampaignId(id:string){return [...this.records.values()].filter(item=>item.campaignId===id)}async list(){return [...this.records.values()]}async upsert(analysis:LeadAnalysis){const current=this.records.get(analysis.leadId);const next={...analysis,id:current?.id??analysis.id,createdAt:current?.createdAt??analysis.createdAt};this.records.set(analysis.leadId,next);return next}}
export class InMemoryIntelligenceActivityRepository implements IntelligenceActivityRepository{readonly activities:Array<{type:"lead_score_calculated"|"lead_analysis_recalculated";leadId:string;campaignId:string;createdAt:string}>=[];async record(activity:(typeof this.activities)[number]){this.activities.push(activity)}}
