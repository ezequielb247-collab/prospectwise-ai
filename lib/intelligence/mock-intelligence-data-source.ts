import {MOCK_LEADS} from "../mock-leads";
import type {LeadIntelligenceDataSource} from "./repositories";
import type {LeadIntelligenceInput} from "./types";
const knownMetrics:Record<string,{rating:number;reviews:number;hasWhatsapp?:boolean;hasInstagram?:boolean;hasAutomationSignals?:boolean}>={"lead-sorriso-prime":{rating:4.8,reviews:46,hasWhatsapp:true,hasAutomationSignals:false},"lead-studio-helena":{rating:4.7,reviews:31,hasInstagram:true},"lead-cafe-aurora":{rating:4.6,reviews:28,hasWhatsapp:true},"lead-vitta-pilates":{rating:4.5,reviews:19},"lead-almeida-reis":{rating:4.4,reviews:15}};
function toInput(lead:(typeof MOCK_LEADS)[number]):LeadIntelligenceInput{const metrics=knownMetrics[lead.id]??{};return {id:lead.id,campaignId:lead.campaignId,name:lead.name,category:lead.category,city:lead.city,website:lead.site?`https://${lead.id}.example`:null,phone:lead.phone,address:lead.city,digitalPresenceLimited:!lead.site,...metrics}}
export class MockLeadIntelligenceDataSource implements LeadIntelligenceDataSource{async findLeadById(id:string){const lead=MOCK_LEADS.find(item=>item.id===id);return lead?toInput(lead):undefined}async findLeadsByCampaignId(id:string){return MOCK_LEADS.filter(lead=>lead.campaignId===id).map(toInput)}async listLeads(){return MOCK_LEADS.map(toInput)}}
