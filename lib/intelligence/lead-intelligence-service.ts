import {LeadScoringEngine} from "./lead-scoring-engine";
import type {IntelligenceActivityRepository,LeadAnalysisRepository,LeadIntelligenceDataSource} from "./repositories";
export class LeadIntelligenceService{constructor(private readonly dataSource:LeadIntelligenceDataSource,private readonly engine:LeadScoringEngine,private readonly analyses:LeadAnalysisRepository,private readonly activities:IntelligenceActivityRepository){}async getAnalysis(leadId:string){return this.analyses.findByLeadId(leadId)}async analyzeLead(leadId:string,options:{recalculate?:boolean}={}){const lead=await this.dataSource.findLeadById(leadId);if(!lead)throw new Error("Lead não encontrado.");const existing=await this.analyses.findByLeadId(leadId);if(existing&&!options.recalculate)return existing;const analysis=this.engine.analyze(lead);const saved=await this.analyses.upsert(analysis);await this.activities.record({type:existing?"lead_analysis_recalculated":"lead_score_calculated",leadId,campaignId:lead.campaignId,createdAt:saved.analyzedAt});return saved}async analyzeCampaign(campaignId:string){const leads=await this.dataSource.findLeadsByCampaignId(campaignId);return Promise.all(leads.map(lead=>this.analyzeLead(lead.id)))}async getCampaignAnalyses(campaignId:string){return this.analyses.findByCampaignId(campaignId)}}
