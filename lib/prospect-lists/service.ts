import type {ProspectList,ProspectListItem} from "./types";
export interface ProspectListRepository{list(userId:string):Promise<ProspectList[]>;get(userId:string,id:string):Promise<ProspectList|undefined>;create(userId:string,input:{name:string;description?:string|null;campaignId?:string|null}):Promise<ProspectList>;rename(userId:string,id:string,name:string):Promise<ProspectList>;delete(userId:string,id:string):Promise<void>;add(userId:string,listId:string,leadIds:string[]):Promise<ProspectListItem[]>;remove(userId:string,listId:string,leadId:string):Promise<void>}
const clean=(value:string,max:number)=>value.replace(/[<>]/g,"").trim().slice(0,max);
export class ProspectListService{constructor(private repo:ProspectListRepository){}list(userId:string){return this.repo.list(userId)}get(userId:string,id:string){return this.repo.get(userId,id)}create(userId:string,input:{name:string;description?:string|null;campaignId?:string|null}){const name=clean(input.name,120);if(!name)throw new Error("Nome da lista é obrigatório.");return this.repo.create(userId,{...input,name,description:input.description?clean(input.description,500):null})}rename(userId:string,id:string,name:string){const safe=clean(name,120);if(!safe)throw new Error("Nome da lista é obrigatório.");return this.repo.rename(userId,id,safe)}add(userId:string,listId:string,leadIds:string[]){const unique=[...new Set(leadIds)];if(!unique.length)throw new Error("Selecione pelo menos um lead.");return this.repo.add(userId,listId,unique)}remove(userId:string,listId:string,leadId:string){return this.repo.remove(userId,listId,leadId)}delete(userId:string,id:string){return this.repo.delete(userId,id)}}
