import Link from "next/link";
import {loginAction} from "../auth/actions";
import {hasSupabaseConfig} from "../../lib/supabase/config";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const params=await searchParams;const configured=hasSupabaseConfig();return <main className="form-page login-page"><div className="form-card"><span className="brand-mark">P</span><h1>Bem-vindo ao ProspectWise</h1><p>Entre para acessar seus próprios dados de prospecção.</p>{params.error&&<div className="search-error">{params.error}</div>}{params.message&&<div className="compliance">{params.message}</div>}{configured?<form action={loginAction}><label>E-mail<input name="email" type="email" required autoComplete="email"/></label><label>Senha<input name="password" type="password" required minLength={8} autoComplete="current-password"/></label><button className="primary full">Entrar</button></form>:<div className="compliance">Modo demonstração local ativo. Configure o Supabase para habilitar contas reais.</div>}<div className="auth-links">{!configured&&<Link className="primary full" href="/dashboard">Abrir demonstração</Link>}<Link href="/cadastro">Criar conta</Link><Link href="/recuperar-senha">Esqueci minha senha</Link></div></div></main>}
