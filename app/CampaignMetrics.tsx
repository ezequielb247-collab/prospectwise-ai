import Link from "next/link";
import {campaignSnapshot,MOCK_CAMPAIGNS} from "../lib/mock-campaigns";
export default function CampaignMetrics(){return <article className="panel leads-panel campaign-metrics-table"><div className="panel-head"><div><h3>Métricas por campanha</h3><p>Visão consolidada de prospecção e conversão</p></div><Link href="/campanhas">Ver campanhas →</Link></div><div className="table-wrap"><table><thead><tr><th>Campanha</th><th>Empresas</th><th>Mensagens</th><th>Respostas</th><th>Clientes</th><th>Progresso</th></tr></thead><tbody>{MOCK_CAMPAIGNS.map(campaign=>{const snapshot=campaignSnapshot(campaign.id)!;return <tr key={campaign.id}><td><Link className="details-link" href={`/campanhas/${campaign.id}`}>{campaign.name}</Link></td><td>{snapshot.metrics.companies}</td><td>{snapshot.metrics.messages}</td><td>{snapshot.metrics.responses}</td><td>{snapshot.metrics.clients}</td><td><b>{snapshot.metrics.progress}%</b></td></tr>})}</tbody></table></div></article>}
