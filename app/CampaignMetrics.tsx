import Link from "next/link";
import type {WorkspaceCampaign} from "../lib/workspace-model";
export default function CampaignMetrics({campaigns}:{campaigns:WorkspaceCampaign[]}){return <article className="panel leads-panel campaign-metrics-table"><div className="panel-head"><div><h3>Métricas por campanha</h3><p>Visão consolidada de prospecção e conversão</p></div><Link href="/campanhas">Ver campanhas →</Link></div><div className="table-wrap"><table><thead><tr><th>Campanha</th><th>Empresas</th><th>Mensagens</th><th>Respostas</th><th>Clientes</th><th>Progresso</th></tr></thead><tbody>{campaigns.map(campaign=><tr key={campaign.id}><td><Link className="details-link" href={`/campanhas/${campaign.id}`}>{campaign.name}</Link></td><td>{campaign.metrics.companies}</td><td>{campaign.metrics.messages}</td><td>{campaign.metrics.responses}</td><td>{campaign.metrics.clients}</td><td><b>{campaign.metrics.progress}%</b></td></tr>)}</tbody></table></div></article>}
