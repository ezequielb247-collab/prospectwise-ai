import{z}from"zod";import{requireApiUser}from"../../../lib/auth/session";import{prospectLists}from"../../../lib/prospect-lists/container";const schema=z.discriminatedUnion("action",[z.object({action:z.literal("create"),name:z.string().min(1),description:z.string().optional(),campaignId:z.string().uuid().nullable().optional()}),z.object({action:z.literal("rename"),id:z.string().uuid(),name:z.string().min(1)}),z.object({action:z.literal("delete"),id:z.string().uuid()}),z.object({action:z.literal("add"),id:z.string().uuid(),leadIds:z.array(z.string().uuid()).min(1)}),z.object({action:z.literal("remove"),id:z.string().uuid(),leadId:z.string().uuid()})]);export async function GET(request:Request){try{const user=await requireApiUser(),id=new URL(request.url).searchParams.get("id"),service=await prospectLists();return Response.json(id?await service.get(user.id,id):await service.list(user.id))}catch(error){return Response.json({error:error instanceof Error?error.message:"Falha ao carregar listas."},{status:400})}}export async function POST(request:Request){try{const user=await requireApiUser(),input=schema.parse(await request.json()),service=await prospectLists();if(input.action==="create")return Response.json(await service.create(user.id,input),{status:201});if(input.action==="rename")return Response.json(await service.rename(user.id,input.id,input.name));if(input.action==="add")return Response.json(await service.add(user.id,input.id,input.leadIds));if(input.action==="remove"){await service.remove(user.id,input.id,input.leadId);return Response.json({ok:true})}await service.delete(user.id,input.id);return Response.json({ok:true})}catch(error){return Response.json({error:error instanceof Error?error.message:"Falha ao salvar lista."},{status:400})}}
