import {requireApiUser} from "../../../../../lib/auth/session";
import {intelligenceForUser} from "../../../../../lib/intelligence/container";
function summary(analyses:Awaited<ReturnType<Awaited<ReturnType<typeof intelligenceForUser>>["service"]["getCampaignAnalyses"]>>){const services=new Map<string,number>();for(const analysis of analyses)for(const service of analysis.recommendedServices)services.set(service.name,(services.get(service.name)??0)+1);return {analyses,averageScore:analyses.length?Math.round(analyses.reduce((sum,item)=>sum+item.score,0)/analyses.length):0,excellent:analyses.filter(item=>item.classification==="Excelente").length,highPriority:analyses.filter(item=>item.priority==="Alta").length,topServices:[...services.entries()].sort((a,b)=>b[1]-a[1]).slice(0,3).map(([name,count])=>({name,count}))}}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const [{id},user]=await Promise.all([params,requireApiUser()]);const {service}=await intelligenceForUser(user.id);return Response.json(summary(await service.getCampaignAnalyses(id)))}catch{return Response.json({error:"Não autenticado."},{status:401})}}
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const [{id},user]=await Promise.all([params,requireApiUser()]);const {service}=await intelligenceForUser(user.id);await service.analyzeCampaign(id);return Response.json(summary(await service.getCampaignAnalyses(id)))}catch(error){return Response.json({error:error instanceof Error?error.message:"Falha ao analisar campanha."},{status:500})}}
