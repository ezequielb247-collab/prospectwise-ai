"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import type {CsvColumnMapping,CsvField,CsvPreview} from "../../../lib/csv-import/types";
const MAX_SIZE=2*1024*1024;
const fields:Array<{value:CsvField|"ignore";label:string}>=[{value:"ignore",label:"Ignorar coluna"},{value:"name",label:"Nome"},{value:"phone",label:"Telefone"},{value:"website",label:"Site"},{value:"address",label:"Endereço"},{value:"city",label:"Cidade"},{value:"state",label:"Estado"},{value:"category",label:"Categoria"},{value:"rating",label:"Nota"},{value:"reviews",label:"Avaliações"},{value:"mapsUrl",label:"Google Maps URL"},{value:"instagram",label:"Instagram"},{value:"facebook",label:"Facebook"}];
type Campaign={id:string;name:string};type ImportResult={imported:number;duplicates:number;invalid:number;leadIds:string[]};
export default function CsvImportPage(){const [campaigns,setCampaigns]=useState<Campaign[]>([]);const [campaignId,setCampaignId]=useState("");const [mode,setMode]=useState<"file"|"paste">("file");const [text,setText]=useState("");const [fileName,setFileName]=useState("");const [mapping,setMapping]=useState<CsvColumnMapping>({});const [preview,setPreview]=useState<Omit<CsvPreview,"rows">>();const [result,setResult]=useState<ImportResult>();const [loading,setLoading]=useState(false);const [error,setError]=useState("");useEffect(()=>{fetch("/api/campaigns").then(response=>response.json()).then(data=>{const items=data.campaigns??[];setCampaigns(items);const requested=new URLSearchParams(window.location.search).get("campaignId");setCampaignId(items.some((item:Campaign)=>item.id===requested)?requested:items[0]?.id??"")}).catch(()=>setError("Não foi possível carregar as campanhas."))},[]);async function readFile(file:File|undefined){setError("");setResult(undefined);setPreview(undefined);if(!file)return;if(file.size>MAX_SIZE){setError("Arquivo maior que 2 MB.");return}if(!file.name.toLowerCase().endsWith(".csv")&&!file.type.includes("csv")){setError("Selecione um arquivo CSV.");return}setFileName(file.name);setText(await file.text())}async function generatePreview(nextMapping=mapping){if(!text.trim()){setError("Selecione um CSV ou cole os dados.");return}setLoading(true);setError("");setResult(undefined);try{const response=await fetch("/api/import/csv/preview",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text,mapping:nextMapping})});const data=await response.json();if(!response.ok)throw new Error(data.error);setPreview(data);const suggestions=Object.fromEntries(data.headers.map((header:{source:string;suggested:CsvField|"ignore"})=>[header.source,nextMapping[header.source]??header.suggested]));setMapping(suggestions)}catch(cause){setError(cause instanceof Error?cause.message:"Falha ao analisar CSV.");setPreview(undefined)}finally{setLoading(false)}}async function commit(){if(!campaignId||!preview)return;setLoading(true);setError("");try{const response=await fetch("/api/import/csv/commit",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({campaignId,text,mapping})});const data=await response.json();if(!response.ok)throw new Error(data.error);setResult(data)}catch(cause){setError(cause instanceof Error?cause.message:"Falha ao importar CSV.")}finally{setLoading(false)}}async function analyzeAll(){setLoading(true);const response=await fetch(`/api/intelligence/campaigns/${campaignId}`,{method:"POST"});setLoading(false);if(!response.ok)setError("Não foi possível analisar os leads agora.")}
return <main className="form-page csv-import-page"><Link href="/leads">← Voltar para leads</Link><section className="csv-import-shell"><article className="panel csv-source"><div className="panel-head"><div><h1>Importar empresas por CSV</h1><p>Revise e confirme os dados antes de salvar.</p></div><a className="secondary compact" href="/modelo-importacao-leads.csv" download>Baixar modelo</a></div><label>Campanha<select value={campaignId} onChange={event=>setCampaignId(event.target.value)}><option value="">Selecione uma campanha</option>{campaigns.map(campaign=><option value={campaign.id} key={campaign.id}>{campaign.name}</option>)}</select></label><div className="csv-mode"><button className={mode==="file"?"active":""} onClick={()=>setMode("file")}>Enviar arquivo</button><button className={mode==="paste"?"active":""} onClick={()=>setMode("paste")}>Colar dados</button></div>{mode==="file"?<label className="csv-drop">CSV de até 2 MB<input type="file" accept=".csv,text/csv" onChange={event=>void readFile(event.target.files?.[0])}/><span>{fileName||"Escolher arquivo CSV"}</span></label>:<label>Dados copiados da planilha<textarea className="csv-paste" value={text} onChange={event=>{setText(event.target.value);setPreview(undefined);setResult(undefined)}} placeholder={'nome,telefone,cidade\nEmpresa Exemplo,19999999999,Campinas'}/></label>}<button className="primary full" disabled={loading||!text||!campaignId} onClick={()=>void generatePreview()}>{loading?"Processando…":"Pré-visualizar dados"}</button>{error&&<div className="search-error">{error}</div>}</article><section className="csv-preview">{preview?<><div className="import-metrics">{[["Linhas válidas",preview.stats.valid,"green"],["Inválidas",preview.stats.invalid,"rose"],["Duplicadas",preview.stats.duplicates,"amber"],["Total",preview.stats.total,"violet"]].map(([label,value,tone])=><article className="panel" key={String(label)}><span className={`metric-dot ${tone}`}/><small>{label}</small><b>{value}</b></article>)}</div><article className="panel"><div className="panel-head"><div><h3>Relacionamento de colunas</h3><p>Confirme como cada coluna será interpretada.</p></div><button className="secondary compact" onClick={()=>void generatePreview(mapping)}>Atualizar prévia</button></div><div className="column-mapping">{preview.headers.map(header=><label key={header.source}><span>{header.source}{!header.recognized&&<em>desconhecida</em>}</span><select value={mapping[header.source]??header.suggested} onChange={event=>setMapping(current=>({...current,[header.source]:event.target.value as CsvField|"ignore"}))}>{fields.map(field=><option value={field.value} key={field.value}>{field.label}</option>)}</select></label>)}</div></article><article className="panel"><h3>Exemplos que serão importados</h3><div className="table-wrap"><table><thead><tr><th>Linha</th><th>Empresa</th><th>Telefone</th><th>Cidade/UF</th><th>Situação</th></tr></thead><tbody>{preview.examples.map(row=><tr key={row.line}><td>{row.line}</td><td>{row.lead.name||"—"}</td><td>{row.lead.phone||"—"}</td><td>{[row.lead.city,row.lead.state].filter(Boolean).join("/")||"—"}</td><td><span className={`badge ${!row.valid?"warning":row.duplicate?"neutral":"success"}`}>{!row.valid?row.errors.join(", "):row.duplicate?"Duplicada":"Válida"}</span></td></tr>)}</tbody></table></div><div className="missing-summary"><b>Campos ausentes</b>{Object.entries(preview.missingFields).filter(([,count])=>count).map(([field,count])=><span key={field}>{fields.find(item=>item.value===field)?.label??field}: {count}</span>)}</div><button className="primary full" disabled={loading||preview.stats.valid===0} onClick={()=>void commit()}>{loading?"Importando…":`Importar ${preview.stats.valid} empresas`}</button></article></>:<div className="empty search-empty"><span>⇧</span><h3>Prévia da importação</h3><p>Os dados normalizados, duplicidades e campos ausentes aparecerão aqui.</p></div>}{result&&<article className="panel import-success"><span>✓</span><h3>{result.imported} empresas importadas</h3><p>{result.duplicates} duplicadas e {result.invalid} inválidas não foram importadas.</p><div className="post-import-actions"><button className="secondary" disabled={loading} onClick={()=>void analyzeAll()}>Analisar todos os leads</button><Link className="primary" href={`/radar?campaignId=${campaignId}`}>Abrir Radar</Link><Link className="secondary" href={`/crm?campaignId=${campaignId}`}>Abrir CRM</Link></div></article>}</section></section></main>}
