import{requireCurrentUser}from"../../lib/auth/session";import{prospectLists}from"../../lib/prospect-lists/container";import ProspectListsPanel from"../ProspectListsPanel";import{WorkspaceShell}from"../ui/interface";export default async function Page(){const user=await requireCurrentUser("/listas"),initial=await(await prospectLists()).list(user.id);return <WorkspaceShell page="listas" title="Listas de prospecção" subtitle="Organize seleções comerciais sem duplicar leads."><ProspectListsPanel initial={initial}/></WorkspaceShell>}
