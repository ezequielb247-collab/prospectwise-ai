"use client";
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */
import {useEffect,useState} from "react";
import type {LeadAnalysis} from "../lib/intelligence/types";
export default function LeadIntelligencePanel({leadId}:{leadId:string}){const [analysis,setAnalysis]=useState<LeadAnalysis>();const [loading,setLoading]=useState(true);const [error,setError]=useState("");async function load(){setLoading(true);setError("");const response=await fetch(`/api/intelligence/leads/${leadId}`);if(response.ok){const data=await response.json();setAnalysis(data.analysis)}else setError("Não foi possível carregar a análise.");setLoading(false)}async function analyze(recalculate=false){setLoading(true);setError("");const response=await fetch(`/api/intelligence/leads/${leadId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({recalculate})});if(response.ok)setAnalysis((await response.json()).analysis);else setError("Não foi possível calcular a inteligência deste lead.");setLoading(false)}useEffect(()=>{void load()},[leadId]);if(loading)return <article className="panel intelligence-panel"><p>Calculando sinais disponíveis…</p></article>;if(error)return <article className="panel intelligence-panel"><p>{error}</p><button className="primary compact" onClick={()=>void load()}>Tentar novamente</button></article>;if(!analysis)return <article className="panel intelligence-panel empty-intelligence"><h3>Inteligência do lead</h3><p>Este lead ainda não foi analisado.</p><button className="primary compact" onClick={()=>void analyze()}>Analisar lead</button></article>;return <article className="panel intelligence-panel"><div className="panel-head"><div><h3>Inteligência do lead</h3><p>Regras {analysis.rulesVersion} · {new Date(analysis.analyzedAt).toLocaleString("pt-BR")}</p></div><button className="secondary compact" onClick={()=>void analyze(true)}>Recalcular</button></div><div className="intelligence-score"><b>{analysis.score}</b><span><strong>{analysis.classification}</strong><small>Prioridade {analysis.priority}</small></span></div><h4>Oportunidades</h4><div className="checks">{analysis.opportunities.map(item=><span key={item}>✓ {item}</span>)}</div><h4>Serviços recomendados</h4><div className="recommendation-list">{analysis.recommendedServices.map(service=><div key={service.rule}><b>{service.name}</b><span className={`badge ${service.priority==="Alta"?"warning":"neutral"}`}>{service.priority}</span><p>{service.reason}</p><small>Regra: {service.rule}</small></div>)}</div><h4>Dados ausentes</h4>{analysis.missingData.length?<div className="checks muted-checks">{analysis.missingData.map(item=><span key={item}>○ {item}</span>)}</div>:<p>Nenhum dado essencial ausente.</p>}</article>}
