"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
export default function Page(){const [done,setDone]=useState(false);const [error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");const form=new FormData(event.currentTarget);const services=form.getAll("services").map(String);try{const response=await fetch("/api/campaigns",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:String(form.get("name")),city:String(form.get("city")),state:String(form.get("state")),segment:String(form.get("segment")),companyLimit:Number(form.get("companyLimit")),dailyLimit:Number(form.get("dailyLimit")),services})});const data=await response.json() as {error?:string};if(!response.ok)throw new Error(data.error??"Não foi possível criar a campanha.");setDone(true)}catch(cause){setError(cause instanceof Error?cause.message:"Falha ao criar campanha.")}}return <main className="form-page"><Link href="/campanhas">← Voltar para campanhas</Link><div className="form-card"><span className="brand-mark">P</span><h1>Nova campanha</h1><p>Campanha, empresas, mensagens e eventos serão relacionados no banco.</p>{done?<div className="success-state"><span>✓</span><h2>Campanha criada!</h2><p>O rascunho foi persistido e já pode receber empresas.</p><Link className="primary" href="/campanhas">Ver campanhas</Link></div>:<form onSubmit={submit}><label>Nome da campanha<input name="name" required placeholder="Ex.: Clínicas odontológicas — Campinas"/></label><div className="row"><label>Cidade<input name="city" required defaultValue="Campinas"/></label><label>Estado<select name="state" defaultValue="SP"><option>SP</option><option>RJ</option><option>MG</option></select></label></div><label>Segmento<input name="segment" required placeholder="Clínicas odontológicas"/></label><label>Serviços oferecidos<div className="checks"><span><input name="services" value="Criação de sites" type="checkbox" defaultChecked/> Criação de sites</span><span><input name="services" value="Bot para WhatsApp" type="checkbox" defaultChecked/> Bot para WhatsApp</span><span><input name="services" value="Agentes de IA" type="checkbox"/> Agentes de IA</span><span><input name="services" value="Automações" type="checkbox"/> Automações</span></div></label><div className="row"><label>Quantidade de empresas<input name="companyLimit" type="number" defaultValue="25" min="1" max="500"/></label><label>Limite diário<input name="dailyLimit" type="number" defaultValue="15" min="1" max="100"/></label></div><div className="safety-note">◉ Nenhum serviço externo será chamado nesta sprint.</div>{error&&<div className="search-error">{error}</div>}<button className="primary full">Criar campanha</button></form>}</div></main>}
