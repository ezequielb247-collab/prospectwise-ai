import Link from "next/link";
import {signupAction} from "../auth/actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="form-page login-page"><div className="form-card"><span className="brand-mark">P</span><h1>Criar conta</h1><p>Seus registros serão isolados dos demais usuários.</p>{error&&<div className="search-error">{error}</div>}<form action={signupAction}><label>Nome<input name="name" required/></label><label>E-mail<input name="email" type="email" required autoComplete="email"/></label><label>Senha<input name="password" type="password" minLength={8} required autoComplete="new-password"/></label><button className="primary full">Cadastrar</button></form><Link href="/login">Já tenho uma conta</Link></div></main>}
