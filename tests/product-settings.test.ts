import assert from"node:assert/strict";import test from"node:test";import{DEFAULT_FLAGS,effectiveFlags,ONBOARDING_STEPS}from"../lib/product-settings";test("providers externos exigem flag e credencial",()=>{assert.equal(effectiveFlags({...DEFAULT_FLAGS,outscraper_enabled:true},{}).outscraper_enabled,false);assert.equal(effectiveFlags({...DEFAULT_FLAGS,outscraper_enabled:true},{OUTSCRAPER_API_KEY:"key"}).outscraper_enabled,true);assert.equal(effectiveFlags({...DEFAULT_FLAGS,google_places_enabled:true},{}).google_places_enabled,false)});test("onboarding contém sete etapas persistíveis",()=>{assert.deepEqual(ONBOARDING_STEPS,["profile","campaign","import","analyze","list","messages","prospecting"])});
