import assert from"node:assert/strict";import test from"node:test";import{RESPONSE_LIBRARY,suggestResponses}from"../lib/responses/library";test("biblioteca cobre objeções comerciais sem IA",()=>{assert.equal(Object.keys(RESPONSE_LIBRARY).length,12);assert.match(suggestResponses("preco")[0],/escopo/i);assert.match(suggestResponses("sem_interesse")[0],/não farei novos contatos/i)});test("sugestões retornadas podem ser editadas sem mutar biblioteca",()=>{const copy=[...suggestResponses("reuniao")]as string[];copy[0]="Editada";assert.notEqual(RESPONSE_LIBRARY.reuniao[0],copy[0])});
