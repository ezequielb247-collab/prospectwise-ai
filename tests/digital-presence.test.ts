import assert from"node:assert/strict";import test from"node:test";import{DigitalPresenceService,type DigitalPresenceRepository}from"../lib/digital-presence/service";import type{DigitalPresence}from"../lib/digital-presence/types";const base:DigitalPresence={leadId:"lead-a",websiteStatus:"unknown",websiteUrl:null,domain:null,httpsEnabled:null,mobileFriendly:null,contactFormPresent:null,whatsappPresent:null,instagramUrl:null,facebookUrl:null,googleMapsUrl:null,googleRating:null,googleReviews:null,googleBusinessStatus:"unknown",lastCheckedAt:null,source:"manual",confidence:50};class Repo implements DigitalPresenceRepository{value?:DigitalPresence;async ownsLead(u:string,id:string){return u==="user-a"&&id==="lead-a"}async get(){return this.value}async save(_u:string,v:DigitalPresence){return this.value=v}}
test("não verificado e não possui persistem como estados distintos",async()=>{const repo=new Repo(),service=new DigitalPresenceService(repo);const unknown=await service.save("user-a",base),absent=await service.save("user-a",{...base,websiteStatus:"absent"});assert.equal(unknown.websiteStatus,"unknown");assert.equal(absent.websiteStatus,"absent")});test("edição valida propriedade e presença exige URL",async()=>{const service=new DigitalPresenceService(new Repo());await assert.rejects(service.save("user-b",base),/não encontrado/);await assert.rejects(service.save("user-a",{...base,websiteStatus:"present"}),/URL/)});
